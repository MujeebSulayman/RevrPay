{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"installCommand": "corepack enable && corepack prepare pnpm@10.7.0 --activate && npm run vercel:install",
	"buildCommand": "npm run vercel:build",
	"outputDirectory": "merchant-frontend/dist",
	"framework": "vite"
}
